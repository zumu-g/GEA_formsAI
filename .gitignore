# Cloned external repos (managed separately, each has its own git)
form_filling_app/
skyvern/

# Claude Code config
.claude/skills/

# Local Claude Code settings
.claude/settings.local.json
.DS_Store
.claude/.DS_Store
secrets/
