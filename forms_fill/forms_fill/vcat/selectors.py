# ponytail: placeholder — populated during the one-time manual portal walkthrough.
# Every selector must carry a comment naming the portal screen it belongs to,
# so drift failures can say "portal layout changed at <screen>" instead of a
# raw Playwright error. Do NOT guess selectors; record them from the real portal.

SELECTORS: dict[str, str] = {}
